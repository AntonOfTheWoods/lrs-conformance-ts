# Deprecated Rewrite Runtime

This directory is reserved for the obsoleted rewritten runtime only.

Rules:

- no active `package.json` scripts may point here
- no normal documentation should advertise this as a runnable code path
- keep this area as archive-only history if legacy implementation files are retained

The active conformance path lives elsewhere; the files in this archive must not be treated as current or preferred code.
